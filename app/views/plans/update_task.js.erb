
alert('THIS SHOULD NOT HAPPEN');
/*var editTaskButton = $('#edit-mode');
var isEditMode = $(editTaskButton).val() == 0 ? false : true;
alert(isEditMode);
isEditMode = !isEditMode;
$(editTaskButton).val(isEditMode ? 1 : 0);

alert(isEditMode);

if (isEditMode)
  $('#edit-task').prop('value', 'Сохранить изменения');
else
  $('#edit-task').prop('value', 'Изменить задачу');

var taskTable = $('#tasks');
$(taskTable).find('.task-checker').each(function() {

  if ($(this).is(':checked'))
  {
    var currentTr = ($(this).closest('tr'));
    $(currentTr).find('.task-form').each(function() {
      if (isEditMode)
        $(this).show();
      else
        $(this).hide();
    })
    $(currentTr).find('.task-label').each(function() {
      if (isEditMode)
        $(this).hide();
      else
      {
        $(this).show();
        setLabelText(this);
      }
    })
  }
});*/
